---
name: stacks/rust
description: Stack profile for Rust — tooling, DoD overlay, and skill routing.
user-invokable: false
---

# Rust

Machine-readable profile: `profile.yaml` in this directory (loaded by `detect-stack.sh`).

## When to read

After stack detection resolves `rust`, read this skill for tooling commands, stack-specific DoD, and pattern skills to load.

## Source of truth

`profile.yaml` — test/lint/security commands, `skills` routing, `dod_overlay`, `mcp_suggest`.
