---
name: stacks/react-native
description: Stack profile for React Native — tooling, DoD overlay, and skill routing.
user-invokable: false
---

# React Native

Machine-readable profile: `profile.yaml` in this directory (loaded by `detect-stack.sh`).

## When to read

After stack detection resolves `react-native`, read this skill for tooling commands, stack-specific DoD, and pattern skills to load.

## Source of truth

`profile.yaml` — test/lint/security commands, `skills` routing, `dod_overlay`, `mcp_suggest`.
