---
name: stacks/nuxt
description: Stack profile for Nuxt.js — tooling, DoD overlay, and skill routing.
user-invokable: false
---

# Nuxt.js

Machine-readable profile: `profile.yaml` in this directory (loaded by `detect-stack.sh`).

## When to read

After stack detection resolves `nuxt`, read this skill for tooling commands, stack-specific DoD, and pattern skills to load.

## Source of truth

`profile.yaml` — test/lint/security commands, `skills` routing, `dod_overlay`, `mcp_suggest`.
