---
name: stacks/svelte
description: Stack profile for Svelte (SPA / Vite) — tooling, DoD overlay, and skill routing.
user-invocable: false
---

# Svelte (SPA / Vite)

Machine-readable profile: `profile.yaml` in this directory (loaded by `detect-stack.sh`).

## When to read

After stack detection resolves `svelte`, read this skill for tooling commands, stack-specific DoD, and pattern skills to load.

## Source of truth

`profile.yaml` — test/lint/security commands, `skills` routing, `dod_overlay`, `mcp_suggest`.
