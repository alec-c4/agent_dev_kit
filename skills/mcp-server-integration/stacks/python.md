# Python MCP

- **SDK** — official `mcp` Python package; FastMCP for quick stdio servers.
- **Async** — asyncio handlers; share DB sessions with FastAPI lifespan where applicable.
- **Validation** — Pydantic models for tool arguments.
- **Deploy** — uv/poetry script entrypoint; document env vars for operators.
- **Security** — read-only tools by default; confirm before adding mutating tools.
