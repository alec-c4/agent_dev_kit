# Project skills

Kit skills deploy here (`./scripts/kit install --target=all --project`).

Antigravity and Codex discover `SKILL.md` files in this directory. Global skills may also live in `~/.agents/skills/` or `~/.gemini/config/skills/` — see [docs/tool-adapters.md](../../docs/tool-adapters.md).
