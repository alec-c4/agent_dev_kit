# Local pack verification notes (optional; never commit)
packs/*/VERIFICATION.local.md

# Local Claude Code overrides (never commit)
.claude/

# Editor / language server (machine-specific)
.ruby-lsp/

# Python
.venv*/
__pycache__/
*.pyc

# OS
.DS_Store

# Node / Bun (kit-runtime devDependencies for typecheck)
node_modules/

# Session planning artifacts — belong to the working repo, not the kit
# (see AGENTS.md § Planning artifacts)
.ai/
